@model IEnumerable<Rayon>

@foreach (var rayon in Model.Where(p => p.Categories.Any()))
{
    <li>
        <a href="#">@rayon.Name</a>
        <ul class="sub-menu">
            @foreach (var category in rayon.Categories)
            {
                <li>
                    <a asp-route="category" asp-route-name="@category.Name.ToSafeUrlString()" asp-route-id="@category.Id">@category.Name</a>
                </li>
            }
        </ul>
    </li>
}
